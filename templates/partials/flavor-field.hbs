<div class="flavor-field">
  <label>{{label}}</label>
  <input type="text" data-field="{{field}}" value="{{value}}">
  <button type="button" data-action="reroll" data-field="{{field}}" title="{{localize 'NPC_GENERATOR.Generator.Reroll'}}">
    <i class="fa-solid fa-dice"></i>
  </button>
</div>
